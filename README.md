spring-data-couchbase-demo
================

The project provides a demonstration of Spring Data Couchbase.


Create a data bucket named `sd-demo` with password `password`

Execute the tests as follows:

```bash
./gradlew test 
```

